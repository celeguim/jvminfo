# test web hook
pipeline {
    agent any
    stages {
        stage('Build for Jenkins Webhook') {
            steps {
                sh 'echo "Update 1: Build completed" '
            }
        }
    }
}
